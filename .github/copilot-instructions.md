Read `AGENTS.md` and `CONTEXT.md` before making repository changes.

Use approved specifications and tickets when they exist.

Prefer small vertical changes that produce observable behavior.

Do not invent architectural decisions when they are absent. Report the missing
decision and return control to the Planner.

Do not commit, push, publish, or create pull requests unless explicitly
requested.
